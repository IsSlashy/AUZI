{
    "version": 2,
    "outputDirectory": "dist/auzi-front",
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "ng build --configuration production",
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }
  